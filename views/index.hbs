<div class="header">
  <h1 class="center-align">{{title}}</h1>
  <h5 class="center-align">{{description}}</h5>
</div>


<!-- Side pane which is where you add new items to the database. -->
<ul id="slide-out" class="side-nav">
  <li><h3>Add to the dictionary</h3></li>
  <li><p>Add a word or phrase</p></li>
  <li><input class="nav-text" id="newtitle" type="text" placeholder="Enter a word"></li>
  <!-- <li><p>Add a sentence</p></li> -->
  <!-- <li><input class="nav-text" id="newdescription" placeholder="Enter a sentence"></li> -->
  <li><button id="submit" class="waves-effect waves-light btn" onclick="submit()">Submit</button></li>

  <br>

  <!-- Area for deleting and listing and saved items in the databse -->
  <li><p>Delete a word/phrase</p></li>
  <ul id="savedWords">
     {{#each list}}
         <li>
             <button class="btn right" onclick="deleteitem({{this.pk_id}})">Delete</button>
             {{#if this.done}}
               <label for="{{this.pk_id}}" onclick="update({{this.pk_id}}, {{this.done}})"></label>
             {{else}}
               <label for="{{this.pk_id}}" onclick="update({{this.pk_id}}, {{this.done}})"></label>
               {{this.title}}
            {{/if}}
         </li>
     {{/each}}
  </ul>
</ul>

<div class="sidebar-nav">
  <a href="#" data-activates="slide-out" class="button-collapse btn waves-effect waves-light"><i class="material-icons">menu</i></a>
  <p>Click here to add more words</p>
</div>

<!-- List of dropdowns for making a sentence -->
<div class="main_body">
  <div class="choice_body row">
    <div class="hoverable input-field col m4 l2">
      <select>
        <option value="" disabled selected>{{option}}</option>
        {{#each list}}
        <option value="{{this.pk_id}}">{{this.title}}</option>
        <li class="divider"></li>
        {{/each}}
      </select>
    </div>
    <div class="hoverable input-field col m4 l2">
      <select>
        <option value="" disabled selected></option>
        {{#each list}}
        <option value="{{this.pk_id}}">{{this.title}}</option>
        <li class="divider"></li>
        {{/each}}
      </select>
    </div>
    <div class="hoverable input-field col m4 l2">
      <select>
        <option value="" disabled selected></option>
        {{#each list}}
        <option value="{{this.pk_id}}">{{this.title}}</option>
        <li class="divider"></li>
        {{/each}}
      </select>
    </div>
    <div class="hoverable input-field col m4 l2">
      <select>
        <option value="" disabled selected></option>
        {{#each list}}
        <option value="{{this.pk_id}}">{{this.title}}</option>
        <li class="divider"></li>
        {{/each}}
      </select>
    </div>
    <div class="hoverable input-field col m4 l2">
      <select>
        <option value="" disabled selected></option>
        {{#each list}}
        <option value="{{this.pk_id}}">{{this.title}}</option>
        <li class="divider"></li>
        {{/each}}
      </select>
    </div>
    <div class="hoverable input-field col m4 l2">
      <select>
        <option value="" disabled selected></option>
        {{#each list}}
        <option value="{{this.pk_id}}">{{this.title}}</option>
        <li class="divider"></li>
        {{/each}}
      </select>
    </div>
  </div>
</div>
